class Solution:
    def maximumElementAfterDecrementingAndRearranging(self, arr):
        n = len(arr)
        arr.sort()
        result = 1
        for i in range(1, n):
            if arr[i] >= result + 1:
                result += 1
        return result
