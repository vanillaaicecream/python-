class Solution:
    def maximumElementAfterDecrementingAndRearranging(self, arr):
        arr.sort()
        current = 1
        for i in range(1, len(arr)):
            if arr[i] > current:
                current = current + 1
        return current
